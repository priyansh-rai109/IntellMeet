# ── Backend URL ───────────────────────────────────────────────────────────────
# Set this to your Render deployment URL (no trailing slash).
# Used for both REST API calls (axios) and Socket.io connections.
# Leave unset for local development — localhost:5000 is the automatic fallback.
VITE_BACKEND_URL=https://your-backend.onrender.com
